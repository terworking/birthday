---
import BaseLayout from '~layouts/BaseLayout.astro'
import BirthdayContent from '~components/BirthdayContent.vue'

import { fetchBirthdayNotificationServer } from '~utils/backend'

const response: BackendListResponse = await fetchBirthdayNotificationServer(
  'list'
).then((response) => response.json())
---

<BaseLayout>
  <BirthdayContent client:only data={response} />
</BaseLayout>
